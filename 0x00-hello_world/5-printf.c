#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: prints a sentence using the function 'printf'
 *
 * Return: 0 (always success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
